[package]
name = "modelparams-examples"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]
members = [
    ".",
    "modelparams",
    "modelparams-derive",
]
resolver = "2"

[dependencies]
modelparams = { path = "modelparams" }

[workspace.lints.rust]
unused_imports = "allow"
